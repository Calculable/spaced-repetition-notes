# Nil-Werte in einem Loop überspringen
🔁

```js
let names = ["Bill", nil, "Ted", nil]

for case let name? in names {
    print(name) //Bill und Ted
}
```

##  Zusammenfassung
- Syntax

#learning unit#